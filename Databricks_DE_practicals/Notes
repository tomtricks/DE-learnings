




----
Rescued Data Column
The rescued data column ensures that rows that don’t match with the schema are rescued instead of being dropped. 
The rescued data column contains any data that isn’t parsed for the following reasons:

The column is missing from the schema.
Type mismatches
Case mismatches

SELECT *
FROM read_files(
        DA.paths_working_dir || '/csv_demo_files/malformed_example_1_data.csv',
        format => "csv",
        sep => "|",
        header => true,
        schema => '''
            order_id INT, 
            email STRING, 
            transactions_timestamp BIGINT''', 
        rescueddatacolumn => '_rescued_data'    -- Create the _rescued_data column
      );

--
TO ingest the rescuded data in the column for the table
SELECT
  cast(_rescued_data:_c0 AS BIGINT) AS order_id from table_name;

-----

eg:

select
CAST(
  COALESCE( 
REPLACE(get_json_object(_rescued_data,'$.price'),"$",''), 
'0'
) as DOUBLE
 )as all_price,
  * from 05_lab_bronze;

--
get_json_object() is the most common way to pull values out of a JSON string


---
Metadata - https://docs.databricks.com/en/ingestion/file-metadata-column.html
------
COPY INTO
https://docs.databricks.com/aws/en/sql/language-manual/delta-copy-into

----
we can use readfiles() function as to create a Table CTAS
like
CREATE TABLE historical_users_bronze_ctas_rf 
SELECT * 
FROM read_files(
        '/Volumes/dbacademy_ecommerce/v01/raw/users-historical',
        format => 'parquet'
      );

------
Read files - https://docs.databricks.com/aws/en/sql/language-manual/functions/read_files

----
Parquet: It’s a columnar storage format that is self-describing. That means the schema (column names, data types, etc.) is embedded inside the file itself.
When you query a Parquet file, the engine (Spark, Hive, DuckDB, etc.) can automatically read the schema without you having to manually specify it.
This is one of the big advantages of Parquet — it’s efficient and schema-aware.

CSV: A CSV file is just plain text with rows and columns separated by delimiters (commas, tabs, etc.). 
It doesn’t carry any metadata about column types. 
So when you load a CSV into a database or query engine, you usually need to define the schema explicitly (e.g., which columns are strings, integers, dates). Otherwise, the system will treat everything as text,
which can cause issues with performance and correctness.


------

SELECT *
FROM parquet.`/path/to/file.parquet`
Spark SQL lets you query Parquet files directly using the file path.



-----


SELECT *
FROM read_files(
       DA.paths_working_dir || '/xml_demo_files/example_1_data.xml',
       format => "xml",
       rowTag => 'book',
       schema => '''
            _id INT, 
            author STRING, 
            title STRING
          ''',
       rescueddatacolumn => '_rescued_data'
     );

The function read_files is part of Databricks’ Auto Loader / file ingestion APIs, which internally leverage Spark’s DataFrame reader capabilities.

-- Drop the table if it exists for demonstration purposes
DROP TABLE IF EXISTS books_bronze_xml;


-- Create the Delta table
CREATE TABLE books_bronze_xml 
AS
SELECT
  _id AS book_id,
  * EXCEPT (_id),
  current_timestamp AS ingestion_timestamp,
  _metadata.file_name AS source_file
FROM read_files(
       DA.paths_working_dir || '/xml_demo_files/example_1_data.xml',
       format => "xml",
       rowTag => 'book',
       schema => '''
            _id INT, 
            author STRING, 
            title STRING
          ''',
       rescuedDataColumn => '_rescued_data'
     );

---

-- View the datatypes of the columns 
DESCRIBE books_bronze_xml

---
DESCRIBE VOLUME dbacademy_ecommerce.v01.raw;
Yes — DESCRIBE VOLUME is exactly how you inspect metadata about a Unity Catalog volume in Databricks.

---

LIST loaction
will listthe folders inside it
---

%python

## Create a volume to store the Auto Loader checkpoint files
spark.sql(f'CREATE VOLUME IF NOT EXISTS dbacademy.{DA.schema_name}.auto_loader_files')

## Set checkpoint location to the volume from above
checkpoint_file_location = f'/Volumes/dbacademy/{DA.schema_name}/auto_loader_files'

## Incrementally (or stream) data using Auto Loader
(spark
 .readStream
   .format("cloudFiles")
   .option("cloudFiles.format", "csv")
   .option("header", "true")
   .option("sep", "|")
   .option("inferSchema", "true")
   .option("cloudFiles.schemaLocation", f"{checkpoint_file_location}")
   .load(f"/Volumes/dbacademy/{DA.schema_name}/csv_files_autoloader_source/")
 .writeStream
   .option("checkpointLocation", f"{checkpoint_file_location}")
   .trigger(once=True)
   .toTable(f"dbacademy.{DA.schema_name}.python_csv_autoloader")
) 

---



